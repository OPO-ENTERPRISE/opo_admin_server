# Archivo de ejemplo de variables de entorno para opo_admin_server
# Copia este archivo como .env y configura los valores reales

# Puerto del servidor de administración (diferente del servidor principal)
PORT=8081

# Ruta base de la API
API_BASE_PATH=/api/v1

# Secret para firmar JWT tokens (genera uno seguro en producción)
JWT_SECRET=admin-jwt-secret-super-seguro-aqui

# Orígenes permitidos para CORS (separados por comas)
# Para aplicaciones móviles:
CORS_ALLOWED_ORIGINS=capacitor://localhost,http://localhost,ionic://localhost,https://localhost
# Para desarrollo web:
# CORS_ALLOWED_ORIGINS=http://localhost:8100,https://tu-dominio.com

# URL de conexión a MongoDB Atlas (mismo que el servidor principal)
DB_URL=mongodb+srv://usuario:password@cluster.mongodb.net/opo?retryWrites=true&w=majority

# Nombre de la base de datos (mismo que el servidor principal)
DB_NAME=opo

# Variables adicionales para producción
NODE_ENV=production
